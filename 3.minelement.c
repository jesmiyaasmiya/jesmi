#include<stdio.h>
int main()
{
	int n,a[50],min,i;
	scanf("%d",&n);
  scanf("%d",&a[1);
  for(i=2;i<=n;i++)
	{
		{
		scanf("%d",&a[i]);
	  }
		if(min>=a[i])
		{
			min=a[i];
		}
}
       printf("%d",min);
       return 0;
}
