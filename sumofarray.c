#include<stdio.h>
int main()
{
int n,k,a[25],sum=0,i,j;
printf("enter the numbers:");
scanf("%d",&n);
printf("enter the values");
for(i=0;i<n;i++)
{
scanf("%d",&a[i]);
}
printf("no. of elements to be added");
scanf("%d",&k);  
for(j=0;i<j;j++)
{
sum=sum+a[j];
}
printf("sum is %d",sum);
return 0;
}
